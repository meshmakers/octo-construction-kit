$schema: https://schemas.meshmakers.cloud/construction-kit-elements.schema.json
types:
- typeId: TreeNode
  derivedFromCkTypeId: ${this}/NamedEntity
  associations:
    - id: ${System}/ParentChild
      targetCkTypeId: ${this}/Tree
    - id: ${System}/ParentChild
      targetCkTypeId: ${this}/TreeNode